// This will have the delete built in

import React from "react";

export default function EditCreator() {
    const { id } = useParams()
    const [creator, setCreator] = useState(null)

    return (
        <>
        </>
    ) 
}